namespace Domain.Models
{
    public class SocialGroup
    {
        //public int SocialGroupId { get; set; }
        //public string GroupName { get; set; }

        //// CreatedBy is the foreign key referencing User
        //public int CreatedBy { get; set; }
        //public virtual User Creator { get; set; }

        //// For the many-to-many, you'll typically have 
        //// a navigation to GroupMemberships or directly to Users if using EF Core 5+ "skip navigations."
        //public virtual ICollection<GroupMembership> GroupMemberships { get; set; }
    }
}
